# seleniummm: Selenium Helper

## Prerequisite
- `python>=3.10` (develop machine environment)
- install `pipenv`

## dependencies
`selenimum`
`chromedriver-autoinstaller`

## How to use
- create virtual environment using pipenv
- `pipenv install`
